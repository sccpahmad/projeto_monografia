pandas>=2.0
numpy>=1.24
requests>=2.31
scipy>=1.10
scikit-learn>=1.3
shap>=0.44
matplotlib>=3.7
pyyaml>=6.0
pyarrow>=12.0
openpyxl>=3.1        # exportação do ranking em Excel (etapa 05)
# Opcional, mas recomendado em redes corporativas (resolve erros de
# certificado SSL ao usar o cert store do sistema operacional):
truststore>=0.9
